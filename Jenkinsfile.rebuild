node {
  checkout scm
  withEnv([
    'USERNAME=test',
    'URL=http://3.82.2.207:7070',
    'JOB=test_deploy',
    'ID=remote',
  ]) 
  {  
   withCredentials(
      [
        string(credentialsId: 'TOKEN', variable: 'TOKEN')        
      ]
    ) {  
        stage('Deploy') {  
              sh "docker exec -it gatsby /bin/bash sh -c 'gatsby build'"    
       }
     }
    }
}



